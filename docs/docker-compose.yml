version: '3'
services:
  jekyll:
#    image: jekyll/jekyll:latest
    build: ./
    ports:
      - "4000:4000"
      - "35729:35729"
    volumes:
      - ./:/srv/jekyll
    command: "bundle exec jekyll serve --host 0.0.0.0 --livereload"
